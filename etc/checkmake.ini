# Allow the existing setup, lint, and coverage recipes while keeping a size limit.
[maxbodylength]
maxBodyLength = 50

# Require the entry points used by this project.
[minphony]
required = help,setup,build,lint,test-unit
